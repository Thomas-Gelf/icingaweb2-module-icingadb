/*
var $summary = $('#col2-summary');
if ($summary.length === 0) {
    $('#layout').append('<div id="col2-summary"></div>');
    $summary = $('#col2-summary');

    $summary.css({
        position: 'absolute',
        top: '5.5em',
        background: 'transparent',
        right: '1.5em',
        width: '10em',
        'z-index': 10,
    });
    $summary.attr('data-base-target', 'col2');

}

icinga.loader.loadUrl('/icingaweb2/icingadb/hosts/summary', $summary);
$summary.show();

*/
